/**
 * SubscriptionGate — pass-through wrapper.
 * 
 * GastroMap is free (donation-supported). This component simply renders
 * its children without any paywall. Kept as a component to avoid breaking
 * existing imports and test mocks.
 */
const SubscriptionGate = ({ children }) => children

export default SubscriptionGate
